added="env_templates/testfile"


changes=(${added} ${modified} ${removed})

for ((i=0; i<${#changes[@]}; i++)); do
	if [[ ${changes[i]} =~ "env_templates" ]] ; then
    	echo "modified! ${changes[i]}"
    else
    	echo "Not modified :("
    fi
done
